#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0),cout.tie(0);
    char c;
    cin >> c;
    cout << (isupper(c)?"upper":"lower") << endl;
    return 0;
}