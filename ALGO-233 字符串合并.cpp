#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0),cout.tie(0);
    string s1,s2;
    while (cin >> s1 >> s2)
    {
        cout << s1 << s2 << endl;
    }
    return 0;
}