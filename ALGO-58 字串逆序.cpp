#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0),cout.tie(0);
    string s;
    getline(cin,s);
    reverse(s.begin(),s.end());
    cout << s << endl;
    return 0;
}